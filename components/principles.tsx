'use client';

export function Principles() {
  return (
    <section className="py-36 px-10 relative">
      <div className="mx-auto max-w-6xl">
        <p className="font-serif text-sm md:text-base italic leading-relaxed text-foreground max-w-md">
          If it breaks, we know why.
          <br />
          If it fails, we see it early.
          <br />
          If it scales, it was designed that way.
        </p>
      </div>
    </section>
  );
}
