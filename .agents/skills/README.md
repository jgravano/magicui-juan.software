# Skills Index (Current Project)

Recommended skills for the current experiments architecture:

- `experiments-home`: iterate `/` home rail, catalog-driven UX, and card styling.
- `resonance-iterate`: tune and evolve `/resonance` with modular boundaries.
- `experiment-scaffold`: scaffold the next experiment route + systems.
- `build-check`: run fast production build and report route integrity.

Legacy skills from older project phases are still present, but the above are the default for this repo state.
